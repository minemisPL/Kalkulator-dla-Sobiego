package me.minemis.calculator.listeners;

import android.view.View;

import me.minemis.calculator.MainActivity;

public class DotListener extends NumberListener{
    public DotListener(MainActivity mainActivity) {
        super(mainActivity);
    }

    @Override
    public void onClick(View view) {

        super.onClick(view);
    }
}
